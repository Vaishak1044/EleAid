# Elephant sample recordings

Place licensed elephant recordings in this folder as `.wav` or `.mp3` files.

When the desktop application starts, it copies files from this folder into:

```text
desktop/data/dataset/Elephant/
```

The runtime copy is used for training. Do not publish recordings unless you
have permission and a clear license for redistribution. Keep this README even
when the folder contains no audio files.
