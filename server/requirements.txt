fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# The API reuses the desktop classifier and therefore also needs the desktop
# runtime dependencies. Install desktop/requirements.txt in the same venv.
